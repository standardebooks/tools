# CLAUDE.md — DEPRECATED

All project documentation has been consolidated into the parent CLAUDE.md at
`../CLAUDE.md`. See that file (section "Tools: ebook build toolset") for the
authoritative reference on the `tl` command, source directory format, metadata
sourcing, and upstream sync.
