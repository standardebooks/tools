"""
This type stub file was generated by pyright.
"""

from . import cssrule

"""CSSPageRule implements DOM Level 2 CSS CSSPageRule."""
__all__ = ['CSSPageRule']
class CSSPageRule(cssrule.CSSRuleRules):
    """
    The CSSPageRule interface represents a @page rule within a CSS style
    sheet. The @page rule is used to specify the dimensions, orientation,
    margins, etc. of a page box for paged media.

    Format::

        page :
               PAGE_SYM S* IDENT? pseudo_page? S*
               '{' S* [ declaration | margin ]?
               [ ';' S* [ declaration | margin ]? ]* '}' S*
               ;

        pseudo_page :
               ':' [ "left" | "right" | "first" ]
               ;

        margin :
               margin_sym S* '{' declaration [ ';' S* declaration? ]* '}' S*
               ;

        margin_sym :
               TOPLEFTCORNER_SYM |
               TOPLEFT_SYM |
               TOPCENTER_SYM |
               TOPRIGHT_SYM |
               TOPRIGHTCORNER_SYM |
               BOTTOMLEFTCORNER_SYM |
               BOTTOMLEFT_SYM |
               BOTTOMCENTER_SYM |
               BOTTOMRIGHT_SYM |
               BOTTOMRIGHTCORNER_SYM |
               LEFTTOP_SYM |
               LEFTMIDDLE_SYM |
               LEFTBOTTOM_SYM |
               RIGHTTOP_SYM |
               RIGHTMIDDLE_SYM |
               RIGHTBOTTOM_SYM
               ;

    `cssRules` contains a list of `MarginRule` objects.
    """
    def __init__(self, selectorText=..., style=..., parentRule=..., parentStyleSheet=..., readonly=...) -> None:
        """
        If readonly allows setting of properties in constructor only.

        :param selectorText:
            type string
        :param style:
            CSSStyleDeclaration for this CSSStyleRule
        """
        ...
    
    def __repr__(self): # -> str:
        ...
    
    def __str__(self) -> str:
        ...
    
    def __contains__(self, margin): # -> bool:
        """Check if margin is set in the rule."""
        ...
    
    def keys(self): # -> list[Any]:
        "Return list of all set margins (MarginRule)."
        ...
    
    def __getitem__(self, margin): # -> None:
        """Retrieve the style (of MarginRule)
        for `margin` (which must be normalized).
        """
        ...
    
    def __setitem__(self, margin, style): # -> int | tuple[Literal[False], Literal[False]] | tuple[Literal[True], Literal[True]] | tuple[Any | CSSRule, int | Any]:
        """Set the style (of MarginRule)
        for `margin` (which must be normalized).
        """
        ...
    
    def __delitem__(self, margin): # -> None:
        """Delete the style (the MarginRule)
        for `margin` (which must be normalized).
        """
        ...
    
    cssText = ...
    selectorText = ...
    style = ...
    def insertRule(self, rule, index=...): # -> int | bool | None:
        """Implements base ``insertRule``."""
        ...
    
    specificity = ...
    type = ...
    wellformed = ...


