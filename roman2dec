#!/usr/bin/python

import sys
import roman

for line in sys.stdin:
	sys.stdout.write(roman.fromRoman(line.upper()))

for argument in sys.argv[1:]:
	sys.stdout.write(roman.fromRoman(argument.upper()))
